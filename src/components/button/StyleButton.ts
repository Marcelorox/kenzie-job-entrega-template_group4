import { styled } from "styled-components";

export const StyleButton = styled.button`
    background-color: var(--blue);
    border-radius: 22px;
    color: var(--white);
    height: 35px;
    padding: 5px 20px;
`